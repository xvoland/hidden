name: Build & Release Hidden Bar (.app)

on:
  push:
    branches: [develop, master]
    tags: ['v*']
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Bump version from tag
        if: startsWith(github.ref, 'refs/tags/')
        run: bash .github/scripts/bump-version.sh "${GITHUB_REF_NAME}"

      - name: Build (ad-hoc, no signing)
        run: >
          xcodebuild -project 'Hidden Bar.xcodeproj'
          -scheme 'Hidden Bar'
          -configuration Release
          CODE_SIGNING_ALLOWED=NO
          -derivedDataPath build

      # Keep the .app bundle intact (the previous upload flattened it to Contents/).
      - name: Package app
        run: ditto -c -k --sequesterRsrc --keepParent "build/Build/Products/Release/Hidden Bar.app" "Hidden Bar.app.zip"

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: Hidden Bar.app
          path: Hidden Bar.app.zip

      # Auto-publish a release only when a version tag is pushed.
      - name: Publish release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: Hidden Bar.app.zip
          prerelease: true
